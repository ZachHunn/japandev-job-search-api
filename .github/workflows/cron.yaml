name: daily-cron
on:
  schedule:
    - cron: "28 11 * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Check for jobs on Japan-dev.com
        run: |
          curl --request GET \
          --url 'https://tokyo-notion-api.vercel.app/api' \
